# Reference hub
Use these docs when you need precise endpoint syntax or troubleshooting flows.

## Requests
Start with `requests/README.md` for curl setup, helper headers, and links to each `/v1/*` endpoint guide.

## Troubleshooting
Open `troubleshooting.md` for error matrices, diagnostics, and escalation paths.

## Cross-links
Role guides (`../admin/`, `../tenant/`, `../user/`) reference this directory whenever they call out API-level work.
