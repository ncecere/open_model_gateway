# Request reference moved
This file now points to the reorganized docs under `docs/reference/requests/`.

Open `requests/README.md` for environment setup, headers, and per-endpoint curl guides, or jump directly to the files inside that directory.
