# Admin Portal

List of Admin Portal UI changes per page

## Dashboard

1. in Gateway health, add color to the chip that show the status.  like the green you use for model status on the models page
2. in Model health, add color to the chip that shows the status of the provider models status. like the green you use for model status on the models page

## Models

1. Show Model Provider icon next to the model alias like in the User Portal

## Tenants

1. for the bar that show amount of budget the tenant has used, make it fill with a green color until it hit the budeget threshold.  when it hits the threshold, make the bar a yellowish orange color.  when it hits budget make the bar red.  this way based on color users can tell but color what tenants are close to running out of budget.


## Users

1. for the bar that show amount of budget the user has used, make it fill with a green color until it hit the budeget threshold.  when it hits the threshold, make the bar a yellowish orange color.  when it hits budget make the bar red.  this way based on color users can tell but color what tenants are close to running out of budget.

## API Keys

no changes at this time

## Usage

no changes at this time

## Files

no changes at this time

## Batches

no changes at this time

## Settings

Instead of different sections in the settings page, can we move the sections into tabs.