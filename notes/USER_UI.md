# User Portal

List of User Portal Changes

Make User portal page sizing match the Admin Portal sizing for element that are in the side bar.  the ones in the user portal seem slightly smaller then the admin ones.

## Dashboard

no changes at this time

## Models

no changes at this time

## Tenants

1. Add budget item to the tenant directory table. this should be like the on in the admin tenant directory
2. for the bar that show amount of budget the user has used, make it fill with a green color until it hit the budeget threshold.  when it hits the threshold, make the bar a yellowish orange color.  when it hits budget make the bar red.  this way based on color users can tell but color what tenants are close to running out of budget.

## API Keys

1. for the bar that show amount of budget the user has used, make it fill with a green color until it hit the budeget threshold.  when it hits the threshold, make the bar a yellowish orange color.  when it hits budget make the bar red.  this way based on color users can tell but color what tenants are close to running out of budget.

## Usage

no changes at this time

## Files

no changes at this time

## Batches

no changes at this time